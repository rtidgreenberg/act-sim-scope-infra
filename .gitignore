# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node / web
node_modules/
dist/

# Docker / build
*.log
build/
tmp/

# EMANE / captures
*.pcap
*.pcapng
*.eel.out

# RTI
rti_license.dat

# rtiddsgen-generated code (router/CMakeLists.txt generates these at build time into
# a build/ dir via connextdds_rtiddsgen_run; RouterAdminTypes.idl is the only source of
# truth — never commit a stray manual `rtiddsgen` run's output alongside it)
router/admin/RouterAdminTypes*.cxx
router/admin/RouterAdminTypes*.h

# Editor / OS
.vscode/
.idea/
.DS_Store

# Reference repos (external, kept locally for analysis — not committed)
references/
Testing/
